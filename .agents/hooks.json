{
  "events": {
    "PreToolUse": [
      {
        "id": "rate-limit-check",
        "description": "Vérifie les quotas API globaux avant toute action sortante.",
        "command": "npm run check:quota",
        "blocking": true,
        "timeoutMs": 5000
      }
    ],
    "PostToolUse": [
      {
        "id": "auto-lint",
        "description": "Déclenche ESLint et Prettier automatiquement après l'écriture ou la modification d'un fichier source.",
        "command": "npm run lint:fix",
        "blocking": false,
        "match": "**/*.ts"
      },
      {
        "id": "state-validation",
        "description": "Vérifie l'intégrité de la base de données locale (SQLite) si un job a été inséré ou modifié.",
        "command": "npm run db:validate",
        "blocking": true,
        "match": "**/state-manager/**"
      }
    ],
    "OnAgentStart": [
      {
        "id": "env-check",
        "description": "Vérifie que le fichier .env contient toutes les clés requises (Z.ai, Gmail OAuth) sans fuite.",
        "command": "npm run env:verify",
        "blocking": true
      }
    ]
  }
}
